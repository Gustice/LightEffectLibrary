#include <stdio.h>
#include <iostream>
#include <stdint.h>

#include "catch.hpp"

/***************************************************************************************************
 * Test Construction
 **************************************************************************************************/
TEST_CASE("Just some test to validate process", "[Test]") // To run: UTeval [Construct]
{
    REQUIRE(true == true);
}

TEST_CASE("Just some other pointless test to validate process", "[Test]") // To run: UTeval [Construct]
{
    REQUIRE(true == true);
}
  
TEST_CASE("And yet another test to validate process", "[Test]") // To run: UTeval [Construct]
{
    REQUIRE(true == true);
}
  